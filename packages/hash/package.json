{
  "name": "@zupple_labs_assignment/hash",
  "module": "index.ts",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/bun": "latest",
    "tsup": "^8.5.0"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts",
    "clean": "rm -rf dist .turbo node_modules"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "bcrypt": "^6.0.0"
  }
}
